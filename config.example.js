// Copy this file to config.local.js for local demo config.
// config.local.js is ignored by git and loaded by index.html before the React island.
window.HANOI_AGENT_CONFIG = {
  openRouterApiKey: '',
  model: 'deepseek/deepseek-v4-pro',
  agentSteps: 6,
};
